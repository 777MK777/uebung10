package gelato.marketing;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class MarketingCoreTest {

    @Test
    @DisplayName("Bei gemäßigtem Wetter (20 °C) an einem Samstag wird Zuppa Inglese angeboten")
    void test_1() {
        // Arrange
//    WetterDienst w = () -> ...;
//    Kalender k = mock(...);
//    when(k.getTag()).thenReturn(...);
//    MarketingCore marketingCore = new MarketingCore(..., ...);

        // Act
//    Mail mail = marketingCore.mailErstellen(...);

        // Assert
//    assertThat(mail.subject()).isEqualTo(...);
//    assertThat(mail.body()).contains(...);
//    assertThat(mail.body()).contains("Hallo ...,");
    }

}
